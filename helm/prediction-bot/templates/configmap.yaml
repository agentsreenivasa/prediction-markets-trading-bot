apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "prediction-bot.fullname" . }}-config
  labels:
    {{- include "prediction-bot.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
