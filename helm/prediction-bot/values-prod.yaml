paperTrading: false

env:
  PAPER_TRADING: "false"

postgres:
  enabled: false
  # Use managed PostgreSQL in production

resources:
  limits:
    cpu: "1"
    memory: 1Gi
  requests:
    cpu: 250m
    memory: 512Mi
